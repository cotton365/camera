# 相机程序核心代码位置说明

## 概述
这是一个基于 GTK2 和 MVSDK (Mindvision SDK) 的相机程序。本文档将帮助您找到核心代码，以便在其他程序中调用。

---

## 一、核心文件位置

### 1. **最重要的核心文件：**

#### `/home/runner/work/camera/camera/GTK_Demo/src/Demo.cpp` (521行)
**这是最核心的相机操作文件！**

包含所有关键的相机功能：
- `camera_init()` - 相机初始化
- `camera_uninit()` - 相机释放
- `read_data()` - 读取相机帧数据
- `preview_thread()` - 预览线程（持续捕获图像）
- 各种相机参数设置函数

#### `/home/runner/work/camera/camera/GTK_Demo/inc/Demo.h`
**核心函数的头文件声明**

---

### 2. **主程序入口：**

#### `/home/runner/work/camera/camera/GTK_Demo/GTK_demo.cpp` (232行)
程序启动文件，包含：
- 相机SDK初始化流程
- 相机枚举和选择
- GUI窗口创建
- 线程启动

---

### 3. **UI回调函数（可选，如果不需要GTK界面可以忽略）：**

#### `/home/runner/work/camera/camera/GTK_Demo/src/callbacks.cpp` (848行)
包含所有GTK界面的按钮回调函数

#### `/home/runner/work/camera/camera/GTK_Demo/src/interface.cpp` (662行)
GTK界面创建代码

---

## 二、核心相机功能函数列表

### A. 基础相机操作（必须）

```cpp
// 1. 相机初始化
void camera_init()
  - CameraIdleStateDevice() - 枚举所有空闲相机
  - CameraInit() - 初始化相机
  - CameraGetCapability() - 获取相机能力
  - CameraPlay() - 开始采集
  - CameraSetIspOutFormat() - 设置输出格式（MONO8或RGB8）

// 2. 相机释放
void camera_uninit()
  - CameraUnInit(g_hCamera) - 释放相机资源

// 3. 读取一帧图像
void read_data(GtkWidget *widget)
  - CameraGetImageBuffer() - 从相机获取原始帧
  - CameraImageProcess() - RAW转RGB处理
  - CameraReleaseImageBuffer() - 释放缓冲区

// 4. 连续采集线程
void * preview_thread(void* arg)
  - 循环调用 read_data() 持续获取图像
```

### B. 相机参数设置

```cpp
// 分辨率设置
int Gtk_SetResolution(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraGetImageResolution() / CameraSetImageResolution()

// 白平衡设置（彩色相机）
int Gtk_SetWB(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraGetGain() / CameraSetGain() - RGB增益
  - CameraGetSaturation() / CameraSetSaturation() - 饱和度

// 曝光控制
int Gtk_SetExposure(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraGetAeState() / CameraSetAeState() - 自动曝光开关
  - CameraGetExposureTime() / CameraSetExposureTime() - 曝光时间
  - CameraGetAnalogGain() / CameraSetAnalogGain() - 模拟增益

// 伽马和对比度
int Gtk_SetGamma_Contrast(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraGetGamma() / CameraSetGamma()
  - CameraGetContrast() / CameraSetContrast()

// 图像处理
int Gtk_SetISPProce(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraGetMirror() / CameraSetMirror() - 镜像翻转
  - CameraGetSharpness() / CameraSetSharpness() - 锐度

// 帧率控制
int Gtk_SetSpeed(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraGetFrameSpeed() / CameraSetFrameSpeed()

// 触发模式
int Gtk_SetTrigger(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraGetTriggerMode() / CameraSetTriggerMode()
    * 模式0：连续采集
    * 模式1：软件触发
    * 模式2：硬件触发

// 图像保存
int Gtk_SetSnap(int hCamera, tSdkCameraCapbility * pCameraInfo)
  - CameraSaveImage() - 保存BMP或RAW格式图像
```

---

## 三、如何提取核心代码在其他程序中使用

### 方案一：直接复用核心文件（推荐）

#### 需要的文件：
1. `GTK_Demo/src/Demo.cpp` - 核心实现
2. `GTK_Demo/inc/Demo.h` - 头文件
3. MVSDK库文件和头文件（需要从SDK获取）

#### 需要移除的部分：
- 去掉所有 GTK 相关的代码（`GtkWidget *widget` 参数）
- 去掉 UI 相关的代码（如 `gtk_widget_queue_draw()`）

#### 保留的核心部分：
```cpp
// 保留这些全局变量
int g_hCamera;                    // 相机句柄
tSdkCameraCapbility g_tCapability; // 相机能力
unsigned char* g_pRgbBuffer;       // RGB缓冲区
unsigned char* g_readBuf;          // 读取缓冲区

// 保留这些核心函数
void camera_init();               // 初始化
void camera_uninit();             // 释放
void read_data();                 // 读取一帧
void* preview_thread(void* arg);  // 预览线程

// 所有 Gtk_Set* 函数去掉 GTK 参数后可以直接使用
```

---

### 方案二：创建独立的相机库

#### 创建新的头文件 `camera_core.h`：
```cpp
#ifndef _CAMERA_CORE_H_
#define _CAMERA_CORE_H_

#include "CameraApi.h"

// 初始化相机
int camera_init(int camera_index);

// 释放相机
void camera_uninit();

// 获取一帧图像
int camera_get_frame(unsigned char** rgb_buffer, int* width, int* height);

// 设置分辨率
int camera_set_resolution(int width, int height);

// 设置曝光时间（微秒）
int camera_set_exposure(double exposure_us);

// 设置模拟增益
int camera_set_gain(int gain);

// 保存图像
int camera_save_image(const char* filename, int format);

#endif
```

#### 创建实现文件 `camera_core.cpp`：
从 `Demo.cpp` 中提取核心函数，去掉 GTK 依赖。

---

## 四、依赖项说明

### 必需的外部库：

#### 1. **MVSDK (Mindvision SDK)** - 最关键！
- 这是迈德威视相机的SDK库
- 库文件位置：`../../lib/libMVSDK.so` 或系统库路径
- 头文件位置：`../../include/`
  - `CameraApi.h` - 相机API接口
  - `CameraDefine.h` - 定义和结构体
  - `CameraStatus.h` - 状态码

#### 2. **pthread** - 多线程支持
- 用于 `preview_thread` 和 `getcounts_thread`

#### 3. **librt** - 实时库
- 用于时间相关函数

#### 4. **GTK 2.0** - 仅用于GUI显示（可选）
- 如果不需要图形界面，可以完全去掉

---

## 五、编译说明

### 当前编译方式：
```bash
cd /home/runner/work/camera/camera/GTK_Demo
./configure
make
./GTK_demo
```

### 提取核心代码后的编译（无GTK）：
```bash
g++ camera_core.cpp -o camera_app \
    -I../../include \
    -L../../lib \
    -lMVSDK \
    -lpthread \
    -lrt
```

---

## 六、核心API使用流程

### 基本使用流程：
```cpp
#include "CameraApi.h"

int main() {
    int hCamera;

    // 1. 初始化SDK
    CameraSdkInit(1);

    // 2. 枚举相机
    tSdkCameraDevInfo tCameraEnumList[10];
    int iCameraCounts = 10;
    CameraEnumerateDevice(tCameraEnumList, &iCameraCounts);

    // 3. 初始化第一个相机
    CameraInit(&tCameraEnumList[0], -1, -1, &hCamera);

    // 4. 获取相机能力
    tSdkCameraCapbility tCapability;
    CameraGetCapability(hCamera, &tCapability);

    // 5. 分配图像缓冲区
    unsigned char* pRgbBuffer;
    pRgbBuffer = (unsigned char*)CameraAlignMalloc(
        tCapability.sResolutionRange.iWidthMax *
        tCapability.sResolutionRange.iHeightMax * 3, 16);

    // 6. 设置输出格式
    CameraSetIspOutFormat(hCamera, CAMERA_MEDIA_TYPE_RGB8);

    // 7. 开始采集
    CameraPlay(hCamera);

    // 8. 循环获取图像
    while(1) {
        tSdkFrameHead sFrameInfo;
        BYTE* pbyBuffer;

        // 获取一帧
        if(CameraGetImageBuffer(hCamera, &sFrameInfo, &pbyBuffer, 1000) == CAMERA_STATUS_SUCCESS) {
            // 图像处理
            CameraImageProcess(hCamera, pbyBuffer, pRgbBuffer, &sFrameInfo);

            // 这里可以使用 pRgbBuffer 中的图像数据
            // 例如：保存、显示、处理等

            // 释放缓冲区
            CameraReleaseImageBuffer(hCamera, pbyBuffer);
        }
    }

    // 9. 停止并释放
    CameraUnInit(hCamera);
    CameraAlignFree(pRgbBuffer);

    return 0;
}
```

---

## 七、重要的全局变量和数据结构

### 在 Demo.cpp 中定义的关键变量：
```cpp
// 相机句柄
int g_hCamera;

// 相机能力结构体
tSdkCameraCapbility g_tCapability;

// 图像缓冲区
unsigned char* g_pRgbBuffer;     // RGB处理后的缓冲区
unsigned char* g_readBuf;        // 用于显示的缓冲区

// 相机状态
int g_display_state = 1;         // 预览状态 (1=运行, 0=停止)

// 线程同步
pthread_mutex_t g_CamListLock;   // 互斥锁保护图像数据

// 相机格式
int g_DispFormat;                // 显示格式（MONO8或RGB8）
int g_EyeCount;                  // 通道数（1=黑白, 3=彩色）
```

---

## 八、核心函数调用关系图

```
main() [GTK_demo.cpp]
  ├─ CameraSdkInit()
  ├─ CameraEnumerateDevice()
  ├─ CameraInit()
  ├─ camera_init() [Demo.cpp]
  │   ├─ CameraGetCapability()
  │   ├─ CameraAlignMalloc()
  │   ├─ CameraSetIspOutFormat()
  │   └─ CameraPlay()
  ├─ pthread_create(preview_thread) [Demo.cpp]
  │   └─ 循环调用 read_data()
  │       ├─ CameraGetImageBuffer()
  │       ├─ CameraImageProcess()
  │       └─ CameraReleaseImageBuffer()
  └─ gtk_main()
```

---

## 九、推荐的提取步骤

### 第1步：复制核心文件
```bash
# 创建新的项目目录
mkdir my_camera_project
cd my_camera_project

# 复制核心源文件
cp GTK_Demo/src/Demo.cpp ./camera_core.cpp
cp GTK_Demo/inc/Demo.h ./camera_core.h
```

### 第2步：修改代码去除GTK依赖
- 删除所有 `#include <gtk/gtk.h>`
- 删除所有 `GtkWidget*` 参数
- 删除所有 `gtk_*` 函数调用
- 保留所有 `Camera*` 函数调用

### 第3步：创建简单的测试程序
```cpp
// test_camera.cpp
#include "camera_core.h"
#include <stdio.h>

int main() {
    printf("初始化相机...\n");
    camera_init();

    printf("开始采集...\n");
    // 采集100帧
    for(int i = 0; i < 100; i++) {
        read_data();
        printf("已采集第 %d 帧\n", i+1);
    }

    printf("释放相机...\n");
    camera_uninit();

    return 0;
}
```

### 第4步：编译和测试
```bash
g++ test_camera.cpp camera_core.cpp \
    -I../../include \
    -L../../lib \
    -lMVSDK \
    -lpthread \
    -lrt \
    -o test_camera

./test_camera
```

---

## 十、常见问题

### Q1: 找不到 MVSDK 库怎么办？
A: 需要安装迈德威视相机的SDK，通常从官网下载：
- Linux版本：libMVSDK.so
- Windows版本：MVSDK.dll
- 确保库文件在系统路径或指定 `-L` 路径

### Q2: 相机初始化失败？
A: 检查：
- 相机是否连接？运行 `lsusb` 查看USB设备
- 权限是否足够？可能需要 `sudo` 运行
- SDK是否正确安装？

### Q3: 如何在其他语言中使用（如Python）？
A: 可以：
1. 使用 ctypes 直接调用 MVSDK 的 C API
2. 将核心代码编译成动态库 (.so)，然后用 ctypes 调用
3. 使用 SWIG 或 pybind11 创建 Python 绑定

---

## 总结

**最核心的文件是：**
1. `GTK_Demo/src/Demo.cpp` - 包含所有相机操作函数
2. `GTK_Demo/inc/Demo.h` - 函数声明

**最关键的函数是：**
1. `camera_init()` - 初始化相机
2. `read_data()` - 读取一帧图像
3. `camera_uninit()` - 释放资源

**必需的依赖：**
- MVSDK 库（libMVSDK.so）

只需要提取这些核心部分，去掉 GTK 界面代码，就可以在其他程序中使用了！
